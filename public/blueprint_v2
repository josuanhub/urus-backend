// CONFIG GLOBAL DEL SISTEMA (DEV / PROD + FEATURE FLAGS + VERSION UI)

export const ENV = {
  MODE: location.hostname.includes("localhost") ? "dev" : "prod",
};

export const API = {
  BASE:
    ENV.MODE === "dev"
      ? "https://urus-backend-production.up.railway.app"
      : "https://urus-backend-production.up.railway.app",
};

export const FEATURES = {
  REALTIME: true,
  AI_AUTOREPLY: false,
  AUTO_FOLLOWUP: false,
  CLOSE_BUTTON: true,
  SCHEDULE_BUTTON: true,
  NEW_CHAT_UI: true,
};

export const UI = {
  VERSION: "v2", // puedes cambiar a v1, v2, v3
};
